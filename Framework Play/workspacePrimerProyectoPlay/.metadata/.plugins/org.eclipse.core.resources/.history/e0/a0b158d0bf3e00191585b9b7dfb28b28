package guice

import play.mvc.With

trait IdiomaHablado {
  def hablarEspanol(posible: Boolean): String
}

class PersonaNatural extends Idioma{
  
  def hablarEspanol(posible: Boolean) = if(posible) "Si hablan español" else "No hablan español"
  
}